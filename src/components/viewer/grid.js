import styled from 'styled-components';

const Row = styled.div`
  align-items: center;
  display: flex;
  flex-wrap: wrap;
  margin: 8px auto;
`;

const Cell = styled.div`
  padding: 8px;
  text-align: left;
`;

export { Cell, Row };
